# utils.py

# TODO: implementar funciones auxiliares para el ETL.
# - configure_logging(): configurar loggers del pipeline
# - validate_columns(df, expected_cols): verificar columnas faltantes/extras
# - save_checkpoint(data, path) / load_checkpoint(path): opcional para reintentos y restart
# - report_summary(df): valores nulos, distribuciones, simple QA

if __name__ == "__main__":
    print("utils.py: helpers del ETL (logging, validación, informes)")
